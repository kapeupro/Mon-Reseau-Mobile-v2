{
  "name": "resiliamap-api",
  "version": "0.1.0",
  "private": true,
  "description": "ResiliaMap API — Bun + Elysia read-only JSON/GeoJSON over the PostGIS resilience schema. AGPL-3.0-or-later.",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "module": "src/index.ts",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@elysiajs/cors": "^1.1.1",
    "@elysiajs/openapi": "^1.4.15",
    "elysia": "^1.1.26",
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "typescript": "^5.6.3"
  }
}
